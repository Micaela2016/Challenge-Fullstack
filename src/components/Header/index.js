import "./styles.scss";
import logo from "../../assets/logo.png";

const Header = () => {
    return (
        <header>
            <img src={logo} alt="Logo de balance"/>
        </header>
    );
};
export default Header;
